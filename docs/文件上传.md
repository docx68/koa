app设定

```js
import path  from 'path';
import koaBody from 'koa-body'

app.use(koaBody({
  multipart:true,
  formidable:{
      // 文件路径
​    uploadDir:path.join('.',"src/uploads/"),
​    keepExtensions:true 
  }
}));
```

